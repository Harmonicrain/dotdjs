import { MapHellhoundConfig, MapMysteryBoxConfig } from '../../types';

export const warehouseHellhound: MapHellhoundConfig = {
    SPEED_BASE: 0.10, // ~6.0 m/s - Still faster than default but manageable
};

export const warehouseMysteryBox: MapMysteryBoxConfig = {
    COST: 950,
};
