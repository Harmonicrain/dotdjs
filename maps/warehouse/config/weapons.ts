import { MapWeaponOverride } from '../../types';

export const warehouseWeapons: Record<string, MapWeaponOverride> = {
    pistol: {
        base: { damage: 35 } // Slightly more damage for pistol here
    }
};
