# Daily Codebase Analysis — 2026-03-10

**Role**: AI Architect / Analyst
**Focus**: Dead Code Removal, Duplicated Logic, Type Safety, Performance Hygiene

Full scan of all directories: `systems/`, `managers/`, `engine/`, `game/`, `state/`, `network/`, `types/`, `config/`, `maps/`, `ui/`. Prioritized by severity and safety of change.

---

## Issues Found

### 1. Duplicated Material Creation in Visual Managers
**Severity**: Medium (Code Duplication)
**Files**: `managers/visual/DecalManager.ts` (lines 96-102 vs 131-139), `managers/visual/GoreManager.ts` (lines 61-68 vs 150-157)
**Description**: Both `DecalManager` and `GoreManager` define their material creation logic twice — once inline when first needed, and again in a `getMaterial()` getter. The `resourceManager.getMaterial()` call already caches by key, so the second definition is dead duplication. Each pair has identical color/property values.

### 2. Duplicate Position Type Defined 7+ Times
**Severity**: High (Type Duplication)
**Files**: `network/NetworkDeltaCompressor.ts:20` (`StoredPos`), `network/NetworkMessageHandler.ts:25,54`, `types/ui.ts:190,230`
**Description**: The shape `{ x, y, z, rot, pitch }` is defined inline in 7+ places. `StoredPos` exists in `NetworkDeltaCompressor.ts` but is never imported elsewhere. All other files redefine it inline.

### 3. Duplicate `WeaponUpgrade` Type
**Severity**: Medium (Type Duplication)
**Files**: `types/player.ts:33`, `config/weapons/types.ts:3`
**Description**: Identical `WeaponUpgrade = Partial<WeaponConfig> & { name: string }` defined in two files. Some files import from one, some from the other.

### 4. Dead Legacy File: `maps/mapTest/index.ts`
**Severity**: High (Dead Code)
**Files**: `maps/mapTest/index.ts` (129 lines)
**Description**: Entire file marked "LEGACY BUILDER" with comment stating "This file is no longer used at runtime." Contains unused `buildMapTest()` function. Should be deleted.

### 5. Deprecated No-Op Function: `getRuntimeEnvironment()`
**Severity**: Low (Dead Code)
**Files**: `maps/MapTextureResolver.ts:55-58`
**Description**: Function is deprecated, returns `null`, typed as `any | null`, and never called externally. Should be removed.

### 6. `as any` Casts in ZombieAnimationSystem
**Severity**: Medium (Type Safety)
**Files**: `systems/zombie/ZombieAnimationSystem.ts:100,115`
**Description**: `z.mesh as any` is used to access `_glbLoaded` and `_zombieWalkAnim` private properties. Should use a proper extended interface instead.

### 7. Multiple `as any` Casts in NetworkMessageHandler
**Severity**: High (Type Safety)
**Files**: `network/NetworkMessageHandler.ts:244,258,272`
**Description**: Interaction messages (`INTERACT_PERK`, `INTERACT_WALL_BUY`, `INTERACT_PACK_A_PUNCH`) are cast to `any` to extract fields. Should use discriminated union narrowing from the `GameMessage` type.

### 8. `[key: string]: any` in Cached State Interfaces
**Severity**: Medium (Type Safety)
**Files**: `network/NetworkMessageHandler.ts:23,53`
**Description**: `CachedHostState` and `CachedClientState` both include `[key: string]: any` index signatures, defeating type checking on all property access.

### 9. Vector3 Allocations in Hot Paths
**Severity**: Medium (Performance)
**Files**: `managers/HellhoundManager.ts:191-192`, `systems/zombie/ZombieAISystem.ts:445`
**Description**: `new BABYLON.Vector3()` inside distance-check loops in `HellhoundManager.findValidSpawnPosition()`. Also `.clone()` calls for hellhound lunge positions in `ZombieAISystem`. Both violate the "no allocations in update/hot path" rule.

### 10. Incomplete `reset()` in ParticleManager
**Severity**: Medium (Memory Leak Risk)
**Files**: `managers/visual/ParticleManager.ts:676-686`
**Description**: `reset()` only cleans 7 of 14 particle system pools. Missing pools: `debrisPSPool`, `dirtBurstPSPool`, `burstPSPool`, `mistPSPool`, `chunksPSPool`, `brainPSPool`, `houndExplosionPSPool`. These will persist across game restarts.

### 11. Navmesh Parameters Duplicated Across All Maps
**Severity**: Low (Config Duplication)
**Files**: `maps/barn/mapDefinition.ts:90-94`, `maps/mapTest/mapDefinition.ts:89-93`, `maps/warehouse/mapDefinition.ts:110-114`
**Description**: Identical 13-field navmesh parameter block copy-pasted in every map definition. Only `walkableRadius` differs on warehouse (0.3 vs 0.5). Should extract to a shared default constant.

### 12. Identical Perk/PAP Costs in All Map Configs
**Severity**: Low (Config Duplication)
**Files**: `maps/barn/config/gameplay.ts:3-10`, `maps/warehouse/config/gameplay.ts:3-10`, `maps/mapTest/config/gameplay.ts:3-10`
**Description**: All three maps define identical perk costs (Juggernog 2500, Speed Cola 3000, Quick Revive 1500, PAP 5000). Only `STARTING_POINTS` differs. Should extract shared costs to a default and only override per-map.

### 13. Unused `MutableRefObject<T>` Re-definition
**Severity**: Low (Dead Code)
**Files**: `types/index.ts:14-16`
**Description**: Custom `MutableRefObject<T>` duplicates React's built-in type. Should use React's native type directly.

---

## Actionable Prompts for Gemini

Execute these in order. Each is self-contained. Run `npm run build` after each to verify.

---

### Task 1: Delete Dead Legacy File

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: `maps/mapTest/index.ts` is a legacy file that is no longer used at runtime. It contains an unused `buildMapTest()` function (129 lines). The file header says "LEGACY BUILDER" and "This file is no longer used at runtime."

YOUR TASK:
1. Verify that nothing imports from `maps/mapTest/index.ts` — search the entire codebase for any import referencing this file path.
2. If nothing imports it, delete `maps/mapTest/index.ts` entirely.
3. Run `npm run build` to confirm no breakage.
```

### Task 2: Delete Deprecated `getRuntimeEnvironment()`

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: `maps/MapTextureResolver.ts` exports a function `getRuntimeEnvironment()` (lines 55-58) that is deprecated, returns `null`, and is typed `any | null`. It is never called.

YOUR TASK:
1. Search the entire codebase for any usage of `getRuntimeEnvironment`.
2. If it is not imported or called anywhere, remove the function and its JSDoc comment from `maps/MapTextureResolver.ts`.
3. Run `npm run build` to confirm no breakage.
```

### Task 3: Remove Unused `MutableRefObject<T>` Type

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: `types/index.ts` (lines 14-16) defines a custom `MutableRefObject<T>` interface that duplicates React's built-in `React.MutableRefObject<T>`.

YOUR TASK:
1. Search the codebase for all usages of `MutableRefObject` imported from `@/types` or `types/index`.
2. Replace those imports with React's native `MutableRefObject` from `react`.
3. Remove the custom `MutableRefObject<T>` interface from `types/index.ts`.
4. If `MutableRefObject` is only used in UI code (which it should be per AGENTS.md separation rules), this change is safe.
5. Run `npm run build` to confirm.
```

### Task 4: Deduplicate Material Creation in DecalManager

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: `managers/visual/DecalManager.ts` defines blood decal material creation logic twice — once inline in `createBloodDecalOnMesh()` (lines 96-102) and again in `getBloodDecalMaterial()` (lines 131-139). Both use `resourceManager.getMaterial("bloodDecalMat", ...)` with identical factory callbacks.

YOUR TASK:
1. Open `managers/visual/DecalManager.ts`.
2. Make `createBloodDecalOnMesh()` call `this.getBloodDecalMaterial()` instead of duplicating the `resourceManager.getMaterial()` call with inline factory.
3. Keep `getBloodDecalMaterial()` as the single source of truth for creating/caching the blood decal material.
4. Run `npm run build` to confirm no breakage.
```

### Task 5: Deduplicate Material Creation in GoreManager

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: `managers/visual/GoreManager.ts` defines floor gore material creation logic twice — once in `initGoreDiscPool()` (lines 61-68) and again in `getFloorGoreMaterial()` (lines 150-157). Both use `resourceManager.getMaterial("floorGoreMat", ...)` with identical factory callbacks.

YOUR TASK:
1. Open `managers/visual/GoreManager.ts`.
2. Make `initGoreDiscPool()` call `this.getFloorGoreMaterial()` instead of duplicating the material factory inline.
3. Keep `getFloorGoreMaterial()` as the single source of truth.
4. Run `npm run build` to confirm no breakage.
```

### Task 6: Fix Incomplete `reset()` in ParticleManager

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: `managers/visual/ParticleManager.ts` — the `reset()` method (around line 676) only stops/resets 7 of 14 particle system pools. The following pools are NOT reset and will leak across game restarts:
- `debrisPSPool`
- `dirtBurstPSPool`
- `burstPSPool`
- `mistPSPool`
- `chunksPSPool`
- `brainPSPool`
- `houndExplosionPSPool`

YOUR TASK:
1. Open `managers/visual/ParticleManager.ts`.
2. Find the `reset()` method.
3. Look at how the existing pools are reset (typically `.stop()` and reset emitter/start). Follow the same pattern.
4. Add reset logic for all 7 missing pools listed above. For each pool, iterate its entries and call `.stop()` at minimum, matching the pattern used for the existing pools.
5. Run `npm run build` to confirm no breakage.
```

### Task 7: Fix Vector3 Allocations in HellhoundManager Spawn Loop

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules. Pay special attention to Section 5 — NO `new` allocations inside hot paths or loops.

PROBLEM: `managers/HellhoundManager.ts` in `findValidSpawnPosition()` (around lines 191-192) creates `new BABYLON.Vector3()` inside a for-loop to compute distances:
```
const dist = BABYLON.Vector3.Distance(
    new BABYLON.Vector3(spawnPos.x, 0, spawnPos.z),
    new BABYLON.Vector3(playerPos.x, 0, playerPos.z)
);
```
This creates 2 Vector3 objects per iteration.

YOUR TASK:
1. Open `managers/HellhoundManager.ts`.
2. Add two private static scratch vectors at the top of the class (or as module-level constants), e.g.:
   ```typescript
   private static readonly _scratchA = new BABYLON.Vector3();
   private static readonly _scratchB = new BABYLON.Vector3();
   ```
3. In the distance loop, replace the `new Vector3()` calls with `.set()` on the scratch vectors:
   ```typescript
   HellhoundManager._scratchA.set(spawnPos.x, 0, spawnPos.z);
   HellhoundManager._scratchB.set(playerPos.x, 0, playerPos.z);
   const dist = BABYLON.Vector3.Distance(HellhoundManager._scratchA, HellhoundManager._scratchB);
   ```
4. Also check for `.clone()` calls in the same file (around lines 151-154 and 183) and replace with `.copyFrom()` into pre-allocated vectors where possible.
5. Run `npm run build` to confirm no breakage.
```

### Task 8: Create Typed Interface for GLB Zombie Meshes

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules. Section 13 forbids `any` usage.

PROBLEM: `systems/zombie/ZombieAnimationSystem.ts` uses `as any` casts on `z.mesh` (lines 100 and 115) to access `_glbLoaded` and `_zombieWalkAnim` properties that exist on the mesh at runtime but aren't typed.

YOUR TASK:
1. Open `types/index.ts` (or create `types/zombie.ts` if cleaner).
2. Create an interface:
   ```typescript
   export interface GlbZombieMesh extends BABYLON.AbstractMesh {
       _glbLoaded?: boolean;
       _zombieWalkAnim?: BABYLON.AnimationGroup;
   }
   ```
3. Open `systems/zombie/ZombieAnimationSystem.ts`.
4. Replace `const root = z.mesh as any;` with `const root = z.mesh as GlbZombieMesh;` (import the new interface).
5. The optional chaining (`root._glbLoaded && root._zombieWalkAnim`) already handles undefined, so this is safe.
6. Run `npm run build` to confirm.
```

### Task 9: Consolidate `StoredPos` Type for Network Position

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: The position shape `{ x: number; y: number; z: number; rot: number; pitch: number }` is defined inline in 7+ places across network files. `StoredPos` exists in `network/NetworkDeltaCompressor.ts:20` but is never shared.

YOUR TASK:
1. Move the `StoredPos` interface from `network/NetworkDeltaCompressor.ts:20` to `types/network.ts` (create this file if it doesn't exist). Export it.
2. In `network/NetworkDeltaCompressor.ts`, import `StoredPos` from `@/types/network` and remove the local definition.
3. In `network/NetworkMessageHandler.ts`, update `CachedHostState` and `CachedClientState` interfaces to use `StoredPos` for their position fields instead of inline `{ x: number; y: number; z: number; rot: number; pitch: number }`.
4. In `types/ui.ts`, find the `STATE` and `INPUT` message types and replace inline position shapes with `StoredPos` where applicable.
5. Run `npm run build` to confirm no breakage.
```

### Task 10: Remove Duplicate `WeaponUpgrade` Type

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: `WeaponUpgrade` type is defined identically in two places:
- `types/player.ts:33`: `export type WeaponUpgrade = Partial<WeaponConfig> & { name: string };`
- `config/weapons/types.ts:3`: `export type WeaponUpgrade = Partial<WeaponConfig> & { name: string };`

YOUR TASK:
1. Search the codebase for all imports of `WeaponUpgrade`.
2. Decide which file should be the canonical source (prefer `types/player.ts` since types belong in `types/`).
3. Remove the duplicate from `config/weapons/types.ts`.
4. Update any imports that were pointing to `config/weapons/types` to point to `types/player` instead.
5. If `config/weapons/types.ts` has other exports, keep the file but remove only the duplicate type. If it's now empty, delete it.
6. Run `npm run build` to confirm.
```

### Task 11: Extract Default Navmesh Parameters

```text
You are working on a Babylon.js TypeScript game project. Read AGENTS.md first for project rules.

PROBLEM: All map definitions (`maps/barn/mapDefinition.ts`, `maps/mapTest/mapDefinition.ts`, `maps/warehouse/mapDefinition.ts`) copy-paste an identical 13-field `navmeshParameters` block. Only warehouse differs on `walkableRadius` (0.3 vs 0.5).

YOUR TASK:
1. Create a new file `maps/defaultNavParams.ts` that exports a constant:
   ```typescript
   export const DEFAULT_NAV_PARAMS = {
       cs: 0.2, ch: 0.2, walkableSlopeAngle: 45, walkableHeight: 2.0,
       walkableClimb: 0.5, walkableRadius: 0.5, maxEdgeLen: 12,
       maxSimplificationError: 1.3, minRegionArea: 8, mergeRegionArea: 20,
       maxVertsPerPoly: 6, detailSampleDist: 6, detailSampleMaxError: 1,
   } as const;
   ```
2. In `maps/barn/mapDefinition.ts` and `maps/mapTest/mapDefinition.ts`, replace the inline navmeshParameters with:
   ```typescript
   navmeshParameters: { ...DEFAULT_NAV_PARAMS },
   ```
3. In `maps/warehouse/mapDefinition.ts`, use:
   ```typescript
   navmeshParameters: { ...DEFAULT_NAV_PARAMS, walkableRadius: 0.3 },
   ```
4. Update `maps/_template/mapDefinition.ts` similarly.
5. Run `npm run build` to confirm.
```

---

## Summary

| # | Task | Severity | Type | Est. Effort |
|---|------|----------|------|-------------|
| 1 | Delete dead `maps/mapTest/index.ts` | High | Dead Code | 5 min |
| 2 | Delete deprecated `getRuntimeEnvironment()` | Low | Dead Code | 5 min |
| 3 | Remove duplicate `MutableRefObject<T>` | Low | Dead Code | 10 min |
| 4 | Deduplicate DecalManager material | Medium | Duplication | 10 min |
| 5 | Deduplicate GoreManager material | Medium | Duplication | 10 min |
| 6 | Fix incomplete ParticleManager `reset()` | Medium | Memory Leak | 15 min |
| 7 | Fix Vector3 allocations in HellhoundManager | Medium | Performance | 15 min |
| 8 | Type GLB zombie mesh (remove `as any`) | Medium | Type Safety | 10 min |
| 9 | Consolidate `StoredPos` network type | High | Duplication | 20 min |
| 10 | Remove duplicate `WeaponUpgrade` type | Medium | Duplication | 10 min |
| 11 | Extract default navmesh params | Low | Duplication | 15 min |
